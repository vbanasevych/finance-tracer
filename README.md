# Фінансовий трекер (Finance Tracker)

**Лабораторний проєкт з дисципліни "Веб-технології"**

Веб-додаток для відстеження особистих фінансів. Платформа дозволяє користувачам вести облік доходів та витрат, керувати різними рахунками, встановлювати місячні ліміти (бюджети) на конкретні категорії та зручно зберігати фотографії чеків у хмарному сховищі.

## 🛠 Стек технологій
* **Backend:** Java, Spring Boot, Spring Data JPA
* **База даних:** PostgreSQL, Liquibase (управління міграціями)
* **Інфраструктура:** AWS S3 (хмарне збереження файлів)
* **Frontend:** Thymeleaf, Bootstrap 5, JavaScript (File API)

---

## 🚀 Етапи розробки

### Pull Request 1: Базовий MVC, Інфраструктура та UI (✅ Виконано)
На першому етапі закладено фундаментальну архітектуру додатку, налаштовано інфраструктуру та реалізовано базовий користувацький інтерфейс.

**З файлу умов виконано наступні пункти: С1, С6, С7, С15, С19.**

**Що було зроблено:**
* **Моделювання БД:** Створено 5 основних сутностей (`User`, `Account`, `Category`, `Budget`, `Transaction`). Налаштовано міграції бази даних через Liquibase (фіксація схем та ініціалізація тестових даних).
* **Конфігурація середовища:** Налаштовано безпечне підключення до PostgreSQL з використанням `.env` файлу.
* **Хмарна інтеграція (AWS S3):** Реалізовано сервіс для завантаження файлів (фото чеків). Налаштовано публічну політику бакета (Bucket Policy) для миттєвого читання зображень через URL.
* **Бізнес-логіка (CRUD):** Реалізовано повний цикл MVC (репозиторії, сервіси, контролери) для створення, читання, оновлення та видалення рахунків, категорій, бюджетів і транзакцій.
* **Frontend та UI (Bootstrap + Thymeleaf):**
    * Створено адаптивну **Landing Page** з плаваючою кнопкою контактів та базовою SEO-оптимізацією (мета-теги, Open Graph).
    * Розроблено універсальну навігаційну панель (Navbar) для всіх сторінок.
    * Реалізовано форми та таблиці з використанням сітки Bootstrap.
* **Динамічний UI (File API):** Додано JavaScript-обробник з використанням об'єкта `FileReader` для миттєвого прев'ю фотографії чеку перед відправкою на сервер без перезавантаження сторінки.
